#include<stdio.h>
#include<stdlib.h>
struct node
{ int info;
struct node* link;
};
struct node* first;
void main()
{
 struct node* ptr;
 ptr=(struct node*)malloc(sizeof(struct node));
 scanf("%d",&ptr->info);
 ptr->link=first;
 first=ptr;
}
